class Err { message: string; }
async function fail(): void throws Err {
    throw new Err("boom");
}
async function main(): void {
    try {
        await fail();
    } catch (e: Err) {
        console.log("caught");
    }
}
