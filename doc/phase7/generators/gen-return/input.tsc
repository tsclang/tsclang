function* limited(max: i32): Generator<i32> {
    let i: i32 = 0;
    while (i < max) {
        yield i;
        i += 1;
    }
}
const g = limited(3);
let r = g.next();
while (!r.done) {
    console.log(r.value);
    r = g.next();
}
