# Анализ: 00.4 Hello World — desktop

## Тесты
- Запущено: 4 теста
- Результат: 4 зелёных / 0 красных
- Тесты: console-log-string, semicolon-optional, console-log-multi-args, top-level-with-func

## Сверка текста со spec

- «Top-level code — код на верхнем уровне файла выполняется как тело main()» → spec/01-intro.md, spec/02-syntax/ → совпадает
- «Точка с запятой опциональна» → spec/02-syntax/ (Форматирование) → совпадает
- «Это не ASI» → spec/02-syntax/ → совпадает

## Сверка текста с impl

### 00.4.1 (first-file.md)
- «console.log("Hello, World!")» → C-output `printf("Hello, World!\n")` → **совпадает** ✓
- «Точка с запятой опциональна» → проверено тестом semicolon-optional → **совпадает** ✓

### 00.4.3 (c-output.md)
- `console.log("Hello, World!")` → `printf("Hello, World!\n")` → **совпадает** ✓
- `console.log(42)` → `printf("%g\n", 42.0)` → **совпадает** ✓ (подтверждено тестом 14-stdlib/console/log-i32)
- `console.log("count:", 3)` → `printf("count: %g\n", 3.0)` → **совпадает** ✓ (подтверждено тестом console-log-multi-args)
- «Codegen мержит несколько аргументов в одну форматную строку printf» → **совпадает** ✓

### 00.4.7 (top-level-code.md)
- **РАСХОЖДЕНИЕ 1**: Книга утверждает name mangling `greet__String` (двойное подчёркивание, заглавная S). Реально: `greet_string` (одинарное подчёркивание, строчная s). impl использует `mangleParams()` из `src/compiler/types.js:81` — соединяет через `_` (одинарное).
- **РАСХОЖДЕНИЕ 2**: Книга утверждает, что переменные `x` и `name` объявлены на file scope (до main). Реально: все переменные внутри `main()`. Impl генерирует let/const как локальные переменные main.
- **РАСХОЖДЕНИЕ 3**: Книга показывает `// ...` в теле функции вместо реального кода. Реально: полное тело с `tsc_string_concat`, `tsc_string_release`.
- **РАСХОЖДЕНИЕ 4**: Книга пропускает `const` перед `String name`. Реально: `const String name = ...`.
- **РАСХОЖДЕНИЕ 5**: Книга пропускает `tsc_string_release(name)` в конце main. Реально: cleanup вызывается.

## Сверка текста с runtime

### 00.4.3 (c-output.md)
- «TSC_INIT() на desktop — практически no-op» → runtime.h:259 → **РАСХОЖДЕНИЕ 6**: TSC_INIT вызывает `clock_gettime(CLOCK_MONOTONIC, ...)` для инициализации `_tsc_t0` (базовое время для performance.now). Это не no-op, но и не тяжёлая операция. Формулировка «практически no-op» неточна.

### 00.4.4 (cmakelists.md)
- Описание CMakeLists.txt → проверено по `bin/index.ts:1086-1105` → **совпадает** ✓
- «Не перезаписывает существующий CMakeLists.txt» → `if (!existsSync(cmakePath))` → **совпадает** ✓

## Расхождения

| # | Файл | Утверждение в книге | Реальность | Критерий |
|---|------|-------------------|-----------|----------|
| 1 | 00.4.7 | `greet__String` (name mangling) | `greet_string` (одинарное _, lowercase) | impl прав — книга упрощена |
| 2 | 00.4.7 | Переменные на file scope | Переменные внутри main() | impl прав — книга упрощена |
| 3 | 00.4.7 | `// ...` в теле функции | Полный код с concat/release | impl прав — книга скрывает детали |
| 4 | 00.4.7 | `String name` (без const) | `const String name` | impl прав — книга пропустила const |
| 5 | 00.4.7 | Нет cleanup | `tsc_string_release(name)` | impl прав — книга пропустила cleanup |
| 6 | 00.4.3 | «TSC_INIT() — практически no-op» | Вызывает clock_gettime для _tsc_t0 | impl прав — неточно, но близко |

## Вердикт
Правки внесены (2026-05-27). Все 6 расхождений исправлены — книга теперь показывает реальный C-output.
- 00.4.3: уточнена формулировка про TSC_INIT
- 00.4.7: заменён C-output на реальный, добавлены пояснения (name mangling, concat, release, const, переменные внутри main)
