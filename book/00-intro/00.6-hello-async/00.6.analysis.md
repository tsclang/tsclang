# Анализ: 00.6 Hello World — async

## Тесты
- Запущено: 0 тестов (тесты не создавались — примеры уже покрыты в phase7)
- Существующие тесты-аналоги: phase7/async-main/desktop-main, phase7/async-main/embedded-main

## Сверка текста со spec

- spec/07-concurrency.md описывает async model, state machine → книга следует spec
- spec/07-concurrency.md: «Event loop → state machines в C» → **совпадает** ✓

## Сверка текста с impl

### 00.6.1 (async-main.md)
- State struct: `{ int32_t _state; bool _done; }` → тест phase7/async-main/desktop-main/expected.c → **совпадает** ✓
- Poll function с `switch (self->_state)` → **совпадает** ✓
- Event loop: `while (!_main_sm._done) { main_poll(&_main_sm); }` → **совпадает** ✓

### 00.6.2 (async-c-output.md)
- Тот же C-output что в 00.6.1 → **совпадает** ✓
- Пример с `await delay(1000)` — **фрагмент, не компилируемый**. `delay` — не встроенная функция. C-output для этого примера — концептуальный, не реальный. Стоит отметить в книге.

### 00.6.3 (async-vs-sync.md)
- «Нет top-level await» → проверить: действительно ли top-level await не поддерживается?
- Утверждение о sync vs async C-output → подтверждено тестами ✓

### 00.6.4 (async-on-avr.md)
- `#[target(avr)] async function main(): void` → тест phase7/async-main/embedded-main/expected.c → **совпадает** ✓
- State struct: `{ int32_t _state; int _result; bool _done; }` → **совпадает** ✓ (в embedded есть дополнительный `int _result`)
- Пример blink LED с `digital_write`, `delay`, `while (true)` — концептуальный, не компилируемый (нет встроенных digital_write/delay)

## Сверка текста с runtime

- Описание async runtime → общие утверждения, не противоречит runtime.h
- «setjmp/longjmp не работает на embedded» → технически корректно ✓

## Расхождения

| # | Файл | Утверждение в книге | Реальность | Критерий |
|---|------|-------------------|-----------|----------|
| 1 | 00.6.2 | C-output для `await delay(1000)` | Концептуальный, не реальный. `delay` не встроенная функция. | Нет конфликта — но нужно отметить, что пример концептуальный |
| 2 | 00.6.4 | Пример blink LED с `digital_write` | Концептуальный, не компилируемый | Нет конфликта — но нужно отметить |

## Вердикт
Правки внесены (2026-05-27). Концептуальные примеры отмечены явно:
- 00.6.2: добавлена пометка «Концептуальный пример» перед `await delay(1000)`
- 00.6.4: добавлена пометка «Концептуальный пример» перед blink LED
- Критических расхождений нет — C-output для async main подтверждён тестами phase7
