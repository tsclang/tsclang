# 00.3.3 Парсер

Парсер — вторая фаза компиляции. Преобразует токены в AST (Abstract Syntax Tree).

## Вход

Токены от лексера:

```
let x : i32 = 42 ;
```

## Выход — AST

```json
{
  "kind": "VarDecl",
  "name": "x",
  "typeAnnotation": { "kind": "TypeRef", "name": "i32" },
  "init": { "kind": "NumberLiteral", "value": 42 }
}
```

## Узлы AST

Основные типы узлов:

| Узел | Описание |
|------|----------|
| `VarDecl` | Объявление переменной (`let x = 1`) |
| `FuncDecl` | Объявление функции |
| `ClassDecl` | Объявление класса |
| `InterfaceDecl` | Объявление интерфейса |
| `EnumDecl` | Объявление enum |
| `Return` | return-выражение |
| `If` | if / else |
| `For` | for-цикл |
| `ForOf` | for-of цикл |
| `While` | while-цикл |
| `BinaryExpr` | Бинарное выражение (`a + b`) |
| `CallExpr` | Вызов функции (`f(x)`) |
| `MemberExpr` | Доступ к члену (`obj.field`) |
| `NumberLiteral` | Числовой литерал |
| `StringLiteral` | Строковый литерал |
| `BoolLiteral` | true / false |
| `NullLiteral` | null |
| `ArrayLiteral` | `[1, 2, 3]` |
| `ArrowFunc` | Стрелочная функция |
| `Import` | import-выражение |
| `Export` | export-выражение |

## Как работает

Парсер использует recursive descent:

1. Смотрит на текущий токен
2. Определяет, какой синтаксический конструкт начинается
3. Вызывает соответствующую функцию (parseVarDecl, parseFuncDecl, ...)
4. Возвращает узел AST

## Ошибки парсера

- Неожиданный токен: `let = 42` — ожидается идентификатор
- Незакрытая скобка: `function f(` — ожидается `)`
- Некорректный синтаксис: `class { }` — ожидается имя класса

## Реализация

Файл: `src/compiler/parser.js`

Экспортирует функцию `parse(tokens, filename, src)` → `{ ast, errors }`.
