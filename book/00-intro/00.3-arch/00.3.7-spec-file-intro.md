# 00.3.7 Файлы спецификации

Спецификация TSClang состоит из 20 директорий (по фазам):

| Директория | Файлы | Содержимое |
|------------|-------|-----------|
| `spec/01-intro/` | 01-intro.md | Зачем, дизайн-философия, overview, установка |
| `spec/02-syntax/` | 02-syntax.md | Синтаксис, операторы, управляющие конструкции |
| `spec/03-types/` | 03-types.md | Базовые типы: числа, строки, null, массивы, Date, конвертация, Clone, Type Aliases |
| `spec/04-classes/` | 04-classes.md | Классы и объектная система: generics, extension methods, enum, интерфейсы, instanceof, классы |
| `spec/05-memory/` | 05-memory.md, 05b-ownership.md, 05c-for-of-iteration.md, 05d-spread-destructuring-merge.md, 05e-closures.md | Ownership, borrow checker, замыкания, итераторы, присваивание |
| `spec/06-errors/` | 06-errors.md | Обработка ошибок, throws, try/catch |
| `spec/07-concurrency/` | 07-concurrency.md | Async/await, threads, embedded concurrency |
| `spec/08-modules/` | 08-modules.md | Модульная система, C interop, .d.tsc, @platform |
| `spec/09-build/` | 09-build.md, 09b-platform-capabilities.md | Типы проектов, система сборки, CLI, package manager |
| `spec/10-stdlib/` | 10-stdlib.md, 19-stdlib-avr.md, 19-stdlib-fs.md, 19-stdlib-hal.md, 19-stdlib-io.md, 19-stdlib-net.md, 19-stdlib-ws.md | Стандартная библиотека + расширенная stdlib (IO/Net/WS) |
| `spec/11-compiler/` | 11-compiler.md | Архитектура компилятора, IR, методология тестов |
| `spec/12-migration/` | 12-migration.md | Migration guide: TypeScript → TSClang |
| `spec/13-decorators/` | 13-decorators.md | Декораторы: синтаксис, Descriptor API, встроенные декораторы, codegen |
| `spec/14-reactive/` | *(пока пусто)* | Reactive |
| `spec/15-regex/` | *(пока пусто)* | Regex |
| `spec/16-lsp/` | *(пока пусто)* | LSP |
| `spec/17-linter/` | *(пока пусто)* | Linter |
| `spec/18-optimizer/` | *(пока пусто)* | Оптимизатор |

## Файлы тестов

Тесты — основной источник истины. Каждый тест:

```
test/cases/<phase>/<feature>/<name>/
  input.tsc        — входной TSC-код
  expected.c       — ожидаемый C-output
  expected.out     — ожидаемый stdout (если runnable)
  expected.error   — ожидаемое сообщение об ошибке (если error)
```

20 фаз, ~1350 тестов.

## Связь: спецификация ↔ тесты ↔ реализация

```
spec/ → описывает, КАК должно работать
test/ → проверяет, КАК работает на самом деле
src/  → реализация
```

Если spec и test расходятся — это баг. Либо spec неверен, либо реализация, либо тест.

## Эта книга

Эта книга — **новая спецификация**. Она заменяет `doc/` и дополняет `spec/`. Каждое утверждение в книге проверяется:

1. Против тестов — есть ли тест, подтверждающий это?
2. Против спецификации — что говорит spec?
3. Против реализации — что делает код?
4. На всех таргетах — desktop, embedded, async

Если три источника расходятся — разбираем через П1–П3.
