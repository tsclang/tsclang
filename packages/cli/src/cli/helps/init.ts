export const init = `tsclang init — Create a new project

USAGE:
  tsclang init [options]

OPTIONS:
  --type <executable|library>   Project type (default: executable)
  --library                     Shorthand for --type library`;
