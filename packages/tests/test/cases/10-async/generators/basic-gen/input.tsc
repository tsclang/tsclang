function* counter(start: i32) {
    let i: i32 = start;
    while (true) {
        yield i;
        i = i + 1;
    }
}
const gen = counter(1);
console.log(gen.next().value);
console.log(gen.next().value);
console.log(gen.next().value);
