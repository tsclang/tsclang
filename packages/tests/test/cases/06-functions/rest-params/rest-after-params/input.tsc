function multiply(factor: i32, ...nums: i32[]): i32 {
    let result: i32 = 0;
    for (let i: i32 = 0; i < nums.length; i++) {
        result = result + factor * nums[i];
    }
    return result;
}
console.log(multiply(2, 1, 3, 5));
