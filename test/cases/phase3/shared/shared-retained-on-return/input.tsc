// [R] Shared<T> returned from function gets retained when returning a param
class Counter {
    val: i32;
}
function identity(c: Shared<Counter>): Shared<Counter> {
    return c;
}
let counter = new Shared<Counter>();
counter.val = 42;
let result = identity(counter);
console.log(result.val);
