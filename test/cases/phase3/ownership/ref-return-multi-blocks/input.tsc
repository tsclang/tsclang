// [E] Both sources blocked when Ref-return result from multi-param call is alive
class Box { x: i32; }
function pick(a: Ref<Box>, b: Ref<Box>): Ref<Box> {
  return a;
}
let b1 = new Box();
b1.x = 10;
let b2 = new Box();
b2.x = 20;
const r = pick(b1, b2);
b2.x = 99;
