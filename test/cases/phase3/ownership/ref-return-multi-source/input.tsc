// [R] Ref return with multiple Ref params — conservative union
class Box { x: i32; }
function pick(a: Ref<Box>, b: Ref<Box>, flag: bool): Ref<Box> {
  return a;
}
let b1 = new Box();
b1.x = 10;
let b2 = new Box();
b2.x = 20;
const r = pick(b1, b2, true);
console.log(r.x);
