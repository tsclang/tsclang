// [R] Arc<T> returned from function gets retained when returning a param
class Counter {
    val: i32;
}
function identity(c: Arc<Counter>): Arc<Counter> {
    return c;
}
let counter = new Arc<Counter>();
counter.val = 42;
let result = identity(counter);
console.log(result.val);
