class MyError extends Error {
    code: i32;
    constructor(msg: string) {
        super(msg);
        this.code = 42;
    }
}
try {
    throw new MyError("oops");
} catch (e: MyError) {
    console.log(e.message);
    console.log(e.code);
}
