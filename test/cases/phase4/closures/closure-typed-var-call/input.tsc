// [R] Closure with TypeFunc annotation called through variable — return type must be preserved
function makeAdder(n: i32): (i32) => i32 {
    return (x: i32): i32 => x + n;
}
const f: (x: i32) => i32 = makeAdder(5);
const y = f(3);
console.log(y);
