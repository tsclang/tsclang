// [F] Transitive capture: closure returned from function captures parameter
function makeAdder(n: i32): () => i32 {
    return (): i32 => n + 1;
}

const add5 = makeAdder(5);
const add10 = makeAdder(10);
console.log(add5());
console.log(add10());
