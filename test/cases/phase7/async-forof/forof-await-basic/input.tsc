async function processItem(x: i32): i32 {
    return x * 2;
}
async function processAll(arr: i32[]): i32 {
    let total: i32 = 0;
    for (const x of arr) {
        const v = await processItem(x);
        total = total + v;
    }
    return total;
}