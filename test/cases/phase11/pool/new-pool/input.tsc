@embedded.pool(4) class Particle { x: i32; }
function make(): Particle throws Error {
    const p = new Particle();
    p.x = 10;
    return p;
}
try {
    let r = make();
    console.log(r.x);
} catch (e: Error) {
    console.log("full");
}
