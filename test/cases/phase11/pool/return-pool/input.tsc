@embedded.pool(4) class Spark { active: boolean; }
function create(): Spark throws Error {
    let s = new Spark();
    s.active = true;
    return s;
}
try {
    let x = create();
    console.log(x.active);
} catch (e: Error) {
}
