@pool(4) class Spark { active: boolean; }
function test(): void throws Error {
    let a = new Spark();
    a.active = true;
    let b = a;
    if (b != null) {
        console.log(b.active);
    }
}
