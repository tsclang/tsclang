class IOError extends Error {
    constructor(msg: string) { super(msg); }
}

function risky(): i32 throws IOError {
    return 42;
}

function run(flag: boolean): void {
    const x = flag ? risky() : 0;
}
