class IOError { message: string; }
function readFile(path: string): string throws IOError {
    throw new IOError();
}
function main(): i32 {
    try {
        readFile("data.txt");
    } catch (e: MathError) {
        return 1;
    }
    return 0;
}
